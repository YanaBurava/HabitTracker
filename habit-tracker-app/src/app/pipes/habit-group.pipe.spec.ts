import { HabitGroupPipe } from './habit-group.pipe';

describe('HabitGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new HabitGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
